#!/bin/bash -eux

#get the address
export IP_ADDR=$(ip -4 -o addr show dev eth1 | awk '{split($4,a,"/");print a[1]}')
#send it
curl -XPUT http://172.28.128.3:8500/v1/agent/service/register -d "{\"Name\":\"$VM_NAME\",\"Address\":\"$IP_ADDR\"}"
